"""The planned-motion primitives. `api` is the only public surface.

Not a re-export shim: a shim is a second public surface, and then there are two places to look for
the contract. Import `morph.arm.api`.
"""
